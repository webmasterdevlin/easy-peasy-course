import { createStore, reducer } from "easy-peasy";
// import HeroStore from "../heroes/hero-store";
// import VillainStore from "../features/villains/villain-store";

const store = createStore({
  // products: createStore(HeroStore),
  // basket: createStore(VillainStore),
});

export default store;
